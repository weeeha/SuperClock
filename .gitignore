# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.local

# User-supplied photos (kept locally, baked into build via public/)
public/photos/*
!public/photos/.gitkeep

# Admin runtime config (per-Pi state — never checked in)
config/fleet.json
config/admin.json
config/occupancy.json

# Visual brainstorm companion (superpowers)
.superpowers/
