// Stub — replaced by Batch B Task 3 (wf/1-fleet-home). Renders so the new
// route tree is navigable during the fan-out.
export default function FleetHome() {
  return <div className="p-6 text-sm opacity-60">Fleet Home — built in Batch B (Task 3)</div>;
}
