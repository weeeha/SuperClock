// Stub — replaced by Batch B Task 5 (wf/3-add-screen-sheet). Interface is
// fixed so Task 4 (Playlist tab) can wire the trigger before Task 5 lands.
export function AddScreenSheet({ open, onClose }: { open: boolean; onClose: () => void }) {
  void open;
  void onClose;
  return null;
}
